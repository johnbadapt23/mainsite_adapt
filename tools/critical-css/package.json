{
  "name": "adapt-critical-css",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated npm project for the critical-CSS generation pipeline (SESSION-HANDOFF.md S111/S112). Deliberately separate from the theme's own package.json so its Puppeteer/Chromium dependency is never downloaded by the main deploy.yml workflow's npm ci step -- only .github/workflows/critical-css.yml installs this.",
  "main": "generate.js",
  "dependencies": {
    "critical": "^7.2.1"
  }
}
